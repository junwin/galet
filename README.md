## Status

Phase 1 (extraction) complete: the `src/llm` modules and their tests are moved
into this package, provider configuration is resolved through `settings.py`,
and the test suite is green without credentials.
